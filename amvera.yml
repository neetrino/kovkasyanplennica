meta:
  environment: node
  toolchain:
    name: npm
    version: 20

build:
  additionalCommands: bash scripts/amvera-build.sh

run:
  command: npm run start --workspace=@shop/web
  containerPort: 3000
