/** Keys from `about.json` → `description` used on the home About section (split across two columns). */
export const ABOUT_STORY_FIRST_KEYS = ['paragraph1', 'paragraph2'] as const;
export const ABOUT_STORY_SECOND_KEYS = ['paragraph3', 'paragraph4', 'paragraph5'] as const;
