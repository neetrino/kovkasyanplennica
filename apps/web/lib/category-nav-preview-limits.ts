/**
 * Max category slugs per `/api/v1/products/category-nav-previews` request.
 * Keep in sync with `app/api/v1/products/category-nav-previews/route.ts`.
 */
export const CATEGORY_NAV_PREVIEW_MAX_SLUGS_PER_REQUEST = 100;
